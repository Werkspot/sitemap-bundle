framework:
    secret:        test
    router:        { resource: "%kernel.root_dir%/../../../Resources/config/routing.yml" }
    templating:
        engines: ['twig']
    test: ~
    assets: ~

# This configuration can be removed once we go fully symfony ^4.0
twig:
    exception_controller:  twig.controller.exception::showAction
